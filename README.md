# mhrezaei.com

Personal site. Static HTML, CSS and JavaScript — no Jekyll, no build system.
Open `index.html` in a browser and it works.

See [SITE.md](SITE.md) for the layout, how `publications.html` is generated
from `_bibliography/papers.bib`, and how to deploy.
